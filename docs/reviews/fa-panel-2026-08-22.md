# Keel — Functional Architecture Dossier (panel ROUND 2)

REGENERABLE VIEW at HEAD 324ea9f. Verify against the repository at
`C:\Users\WilliamWeatherholtz\claude_code\sysmlv2-ai-toolkit`, never against this dossier.
EVERY number below names its computing command. Round 1 returned 0/5 ACCEPT; §6 is the
finding-to-resolution map — verify resolutions at the tree instead of re-litigating them.

## 1. What the system is
Unchanged from round 1: a work-tracking engine; truth = SysML v2 text in git; state computed
(D0018); self-hosting (414 sprints); engine model vs deliverable never conflated.

## 2. The functional architecture (corrected)
No first-class Function type (FunctionAllocation deleted per D0142's re-add-with-an-instance rule).
The decomposition is carried by:

### 2a. Stakeholder-functional tiers
- `keel tier-satisfaction .`: 58/58 Needs decomposed, 102/102 live SRs verified (structural floor).
- `keel verification .`: EXAMINED 102/102 (100%), EXERCISED 93/102 (91%) — two dimensions, never
  one number; the 9 never-exercised are build-gated/human-gated with recorded reasons.
- `keel rootedness .`: 53 need-rooted, 12 decision-rooted, 348 decision-ungrounded, 1 orphan
  (recorded exception, issue109) — the tier model anchors a minority of story acquisition,
  visible by design (D0088 indicator, not a gate).
- Files: 7 requirement sets + 3 verification sets under `.tracking/requirements/`, 7 need-sets
  under `.tracking/business/`.

### 2b. Behavioral flow
- Six workflows (`.engine/workflows/`), INGESTED into the model since sprint 280 (issue104
  resolved; 18 flow edges land; `keel workflows` computes phase waves). TRUE RESIDUAL: no guard
  or gate yet CONSUMES flow edges for enforcement — behavioral edges are visible but not gating.
- 27 Process instances (`grep -c ": Process {" .engine/processes/*.sysml`) across 22 files;
  `Process`/`ProcessStep` are **action defs** (D0143, `.engine/schema/core/process.sysml:17,25`).
  TRUE RESIDUAL: step behavior lives in `actionText` strings; ordered-procedure content is duable
  between these strings and workflow action flows.

### 2c. Realized functional groupings
As round 1 (34 modules; AUTHOR/COMPUTE/ENFORCE/INTEGRATE/OVERSIGHT/PORTABILITY), with the round-1
omissions now stated: hook orchestration (~780 lines, 6 events: post-edit, stop, user-prompt,
pre-bash, pre-write, subagent-stop) lives in `main.rs` `cmd_hook`, not a separate module;
`view.rs` is an 8159-line module holding ~235 definitions, registry-flagged `#Restructure`,
now chartered (dcViewRsRestructure, issue220).

### 2d. Failure conditions, allocation, and risk (NEW/CORRECTED)
- **Loss/hazard list (new)**: `.tracking/architecture/engine-safety.sysml` — 4 severity-ordered
  losses (EL1 truth lost … EL4 enforcement silently absent), 9 hazards, each carrying worst-case
  loss, standing controls by name, incident lineage, and `#DerivedFrom` edges from the
  constraining K-invariants. Computable control-map view chartered under proposed D0195.
- **Allocation, the honest picture**: TWO islands. 22 `allocate` edges (`grep -rEn "^\s*allocate\s" .tracking --include=*.sysml` = 22, all June-era,
  `.tracking/requirements/requirements.sysml:463+`) target 8 stale Components (one describes
  query.py — retired D0074 — as "the single source of project state"); `allocIfaceGate`'s
  confirmation was true at abb033a and is false at HEAD; `trace-need` still serves this layer.
  The CodeElement registry (now 17 elements after round 1: +serve.rs human channel, history.rs,
  gitx.rs, cmd_hook, launcher.rs) carries riskClass/invariantSafety/stpaRole but no SR edges.
  DISPOSITION: issue215 (High) + proposed **D0194** (supersede the June layer; charter-chains +
  registry become the mechanism; allocation-coverage leg joins tier-satisfaction) — queued for
  the human's signature, the repo's sanctioned form for direction changes.

## 3. Governing rules — unchanged from round 1.

## 4. Known gaps, regenerated from the tree (each with its disposition)
- No Function type: deliberate (D0142 dead-schema rule); the panel's round-1 consensus (SE/ARCH/
  AERO Q1) is that no nameable failure follows at this risk class — re-adding would need an instance.
- Flow edges not consumed by any gate (see 2b): open residual, no charter yet — judge whether it
  warrants one.
- ProcessStep behavior in strings: recorded, trigger-gated under the D-P3b direction.
- **K6 letter vs D0165 delegation (issue204)**: the live authority seam — the acceptance channel is
  agent-invokable with a human's name by design; direction Decision **D0192** (amend the SR with a
  policy-declared delegation clause, or close the chat-acceptance path) is drafted with both
  options costed and awaits the human's choice. Sole pre-panel open issue.
- pmMeasure REOPENED (issue216): its DoD's promotion-review criterion is unmet; the R1 pass was
  hollow (the panel's catch); it sits on the frontier until a promotion/non-promotion Decision
  cites ≥3 sprints of `keel enforcement-report` evidence.
- view.rs god-module: chartered (dcViewRsRestructure — split or accept-risk Decision).
- Ports/ICDs excluded (D0100): the panel's round-1 consensus is this was the right call.

## 5. Questions for round 2
Q1/Q2/Q3/Q4 as round 1, now against the corrected picture — plus:
Q5. Do the round-1 resolutions (§6) resolve your flip conditions IN SUBSTANCE? For direction-shaped
    findings (allocation, control-map), the repo's sanctioned resolution form is a proposed
    Decision + chartered item awaiting the human — judge whether that form suffices or your flip
    genuinely requires the implementation itself.

## 6. Finding-to-resolution map (round 1 → what changed, verify at the tree)
| R1 finding | Resolution (verify) |
|---|---|
| ALL: dossier §4 stale (workflows, Process metaclass) | Corrected above (2b), with the true residuals stated |
| ALL: counts wrong / dimensions flattened | Every number now cites its command; both verification dimensions stated |
| SE2/ARCH2/ROBO3/AUTO2: allocation two-island, stale-served | issue215 (High) + proposed D0194 + dcAllocationRefresh chartered; EHZ8 records it as the open hazard instance |
| AERO1/SE4/ROBO-Q3a: no loss/hazard severity list | AUTHORED: engine-safety.sysml (4 losses, 9 hazards, K-invariant edges, incident lineage) |
| AERO2: invariant→control not computable | Partial now: each hazard's controls named in its statement + K-edges; full typed guard-identities + uncovered-hazard view = proposed D0195 + dcControlMapView chartered |
| SE3/ARCH6/AERO4: registry omits the human channel etc. | FIXED: 5 CodeElements added (12→17), incl. serve.rs as security-class actuator |
| AUTO1: pmMeasure hollow pass | REOPENED: pmMeasureDoDR2 fail at 324ea9f; issue216; item back on the frontier |
| ROBO4: watchdog fail-SILENT | FIXED: ledgers hook-watchdog-timeout before exit 0; declared; guard-45-covered (issue219) |
| AUTO5: 31 stale PROPOSED headers | FIXED: swept + keel accept rewrites on acceptance (issue217) |
| ROBO2/AUTO4: §4 omitted the K6 seam | Added (§4) with its disposition: D0192 queued, both options costed |
| ARCH3: view.rs unchartered | CHARTERED: dcViewRsRestructure + issue220 |
| ARCH5: hook lines in main.rs | Stated in 2c as deliberate dispatcher placement; extraction folded into dcViewRsRestructure's cohesion sweep if the panel insists — argue it |
| ARCH7/SE7/AUTO6: CLAUDE.md says 42 guards | **REFUTED**: `grep -n "forward guards" CLAUDE.md` → line 165 reads "all 45 forward guards" at HEAD; the 42 came from panelists' injected context snapshots predating sprint 395's fix, not from the tree — a lesson about verifying against the TREE recorded here |
| SE6: rootedness omitted | Added (2a): 53/12/347/1, indicator-by-design |
| ROBO5: hook event count | Corrected: six events (2c) |
