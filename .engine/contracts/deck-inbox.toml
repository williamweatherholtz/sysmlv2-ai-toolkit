# The deck's remote inbox (issue194): the mobile transport when keel serve is unreachable.
#
# The page delivers each tap as one ROW to this sheet through the viewer's Smartsheet
# connector (Claude artifact `mcp` capability). The store's reply - a row id - is the
# RECEIPT the page shows: real receiver-side acknowledgment, unlike the retired live-doc
# mechanism whose writes silently dropped. The AI session then pulls unprocessed rows,
# records each through the keel write API, verifies the computed view, and checks
# Processed. Absent file = the deck emits no inbox config and the page says so honestly.
provider = "smartsheet"
sheet_id = 6648284566212484

[columns]
uid = 3044824116924292
name = 3882920144310148
kind = 7548423744294788
verdict = 1918924210081668
note = 6422523837452164
by = 4170724023766916
at = 8674323651137412
deck_head = 511549326528388
processed = 5015148953898884
