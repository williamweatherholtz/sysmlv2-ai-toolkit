{
  "permissions": {
    "deny": [
      "Skill(systems-engineering)",
      "Skill(software-engineering)",
      "Skill(se)",
      "Skill(sw)",
      "Skill(phase-orchestrator)",
      "Skill(arbiter)",
      "Skill(critic-devils-advocate)",
      "Skill(critic-premortem)",
      "Skill(critic-stakeholder)",
      "Skill(critic-traceability)"
    ]
  },
  "outputStyle": "keel",
  "hooks": {
    "UserPromptSubmit": [
      {
        "hooks": [
          {
            "type": "command",
            "shell": "bash",
            "command": "K=$(command -v keel 2>/dev/null); [ -n \"$K\" ] || { [ -x ./target/release/keel.exe ] && K=./target/release/keel.exe; }; [ -n \"$K\" ] || { [ -x ./target/release/keel ] && K=./target/release/keel; }; [ -n \"$K\" ] && \"$K\" hook user-prompt || true",
            "timeout": 30,
            "statusMessage": "route-first checklist"
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "shell": "bash",
            "command": "K=$(command -v keel 2>/dev/null); [ -n \"$K\" ] || { [ -x ./target/release/keel.exe ] && K=./target/release/keel.exe; }; [ -n \"$K\" ] || { [ -x ./target/release/keel ] && K=./target/release/keel; }; [ -n \"$K\" ] && \"$K\" hook post-edit || true",
            "timeout": 120,
            "statusMessage": "in-loop edit gate"
          }
        ]
      }
    ],
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "shell": "bash",
            "command": "K=$(command -v keel 2>/dev/null); [ -n \"$K\" ] || { [ -x ./target/release/keel.exe ] && K=./target/release/keel.exe; }; [ -n \"$K\" ] || { [ -x ./target/release/keel ] && K=./target/release/keel; }; [ -n \"$K\" ] && \"$K\" hook stop || true",
            "timeout": 180,
            "statusMessage": "in-loop honest-state gate"
          }
        ]
      }
    ],
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "shell": "bash",
            "command": "K=$(command -v keel 2>/dev/null); [ -n \"$K\" ] || { [ -x ./target/release/keel.exe ] && K=./target/release/keel.exe; }; [ -n \"$K\" ] || { [ -x ./target/release/keel ] && K=./target/release/keel; }; [ -n \"$K\" ] && \"$K\" hook pre-bash || true",
            "timeout": 15,
            "statusMessage": "shell-adaptation advisory"
          }
        ]
      }
    ]
  }
}
